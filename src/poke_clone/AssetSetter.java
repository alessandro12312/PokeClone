package poke_clone;

import poke_clone.object.OBJ_Chest;
import poke_clone.object.OBJ_Door;
import poke_clone.object.OBJ_Key;

public class AssetSetter {

    GamePanel gp;

    public AssetSetter(GamePanel gp) {
        this.gp = gp;
    }

    // STEP 8.6: Metodo per creare ed inserire gli oggetti all'interno dell'array del gioco.
    // TODO: Implementa il metodo 'public void setObject()'
    // Esempio d'uso:
    // gp.obj[0] = new OBJ_Key();
    // gp.obj[0].x = 4 * gp.TILE_SIZE;
    // gp.obj[0].y = 3 * gp.TILE_SIZE;
    //
    // gp.obj[1] = new OBJ_Door();
    // gp.obj[1].x = 8 * gp.TILE_SIZE;
    // gp.obj[1].y = 5 * gp.TILE_SIZE;
}
